grow_version: ">=0.0.66"

home: /content/pages/home.html

static_dirs:
- static_dir: /assets/
  serve_at: /static/

error_routes:
  default: /views/errors/default.html

localization:
  default_locale: en

preprocessors:
- kind: gulp
  run_task: 'styles'
